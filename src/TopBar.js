import React from 'react';
import './TopBar.css'; // We'll add styles in the next step

const TopBar = () => {
  return (
    <div className="top-bar">
      {/* Placeholder for future content (e.g., user button) */}
    </div>
  );
};

export default TopBar;

